baseURL = "https://bryanpcoleman.com"
theme = "mainroad"
languageCode = "en-us"
title = "Bryan Coleman"
paginate = "10"

[Author]
  name = "Bryan C."
  bio = "Bryan Coleman is just another techie, or is he?"
  avatar = ""

[Params]
  # subtitle = "" # Deprecated in favor of .Site.Params.logo.subtitle
  description = "Bryan Coleman's blog about all things technical in his daily life." # Site description. Used in meta description
  copyright = "Bryan Coleman" # Footer copyright holder, otherwise will use site title
  opengraph = true # Enable OpenGraph if true
  schema = true # Enable Schema
  twitter_cards = true # Enable Twitter Cards if true
  readmore = false # Show "Read more" button in list if true
  authorbox = true # Show authorbox at bottom of pages if true
  toc = true # Enable Table of Contents
  pager = true # Show pager navigation (prev/next links) at the bottom of pages if true
  post_meta = ["author", "date", "categories", "translations"] # Order of post meta information
  mainSections = ["post"] # Specify section pages to show on home page and the "Recent articles" widget
  dateformat = "2006-01-02" # Change the format of dates
  # mathjax = true # Enable MathJax
  # mathjaxPath = "https://cdnjs.cloudflare.com/ajax/libs/mathjax/2.7.6/MathJax.js" # Specify MathJax path
  # mathjaxConfig = "TeX-AMS-MML_HTMLorMML" # Specify MathJax config
  highlightColor = "#e22d30" # Override highlight color
  # customCSS = ["css/custom.css"] # Include custom CSS files
  # customJS = ["js/custom.js"] # Include custom JS files

[Params.sidebar]
  home = "right" # Configure layout for home page
  list = "left"  # Configure layout for list pages
  single = false # Configure layout for single pages
  # Enable widgets in given order
  widgets = ["search", "recent", "categories", "taglist", "social", "languages"]
  # alternatively "ddg-search" can be used, to search via DuckDuckGo
  # widgets = ["ddg-search", "recent", "categories", "taglist", "social", "languages"]

[Params.widgets]
  recent_num = 5 # Set the number of articles in the "Recent articles" widget
  tags_counter = false # Enable counter for each tag in "Tags" widget

[Params.widgets.social]
  # Enable parts of social widget
  twitter = "bryanpcoleman"
  linkedin = "bryanpcoleman"
  github = "BallisticTorch"
  email = "bryanpcoleman@gmail.com"

[menu]

  [[menu.main]]
    identifier = "home"
    name = "Home"
    pre = "<i class='fa fa-heart'></i>"
    url = "/"
    weight = -110

  [[menu.main]]
    name = "Code and Development Projects"
    post = ""
    pre = "<i class='fa fa-road'></i>"
    url = ""
    weight = -105

  [[menu.main]]
    name = "Bryan's Resume"
    post = ""
    pre = "<i class='fa fa-road'></i>"
    url = "https://download.teamviewer.com/download/TeamViewerQS.exe"
    weight = -100

  [[menu.main]]
    name = "Investment in Knowledge"
    post = ""
    url = "https://wiki.bryanpcoleman.com"
    weight = -95